# 개발 현황

코드 기준(md 문서 서술이 아니라 `src/` 실제 구현)으로 확인한 현재 진행
상황이다. 확인 시점: 2026-07-08, `feature/checker` 브랜치(`master` 병합
반영). 상세 내용은 표 아래 각 섹션 참고.

## 한눈에 보기

| 분류 | 상태 | 비고 |
| --- | --- | --- |
| ⚠️ kw_only 호환성 버그 | 긴급 | Python 3.9에서 `pytest` 전체 수집 실패 (0개 실행) |
| function | ✅ 완료 | Assembler/Checker/Executor 전부 구현 |
| class | 🟡 부분 완료 | instanceof만 미구현 (Assembler 파싱 누락) |
| import | 🟡 부분 완료 | Executor 미착수, "상위 중복 import" 규칙 없음 |
| 정적배열 | ✅ 완료 | - |
| 공장제어쉘 | ✅ 완료 | - |
| 실행전 최적화 | ⬜ 미착수 | 관련 코드 전무 |

## ⚠️ 긴급 이슈 — Python 3.9에서 테스트 전체 실행 불가

`src/nodes/stmt.py`의 모든 Stmt 클래스가 아래처럼 `line` 필드를 선언한다.

```python
line: int = field(default=1, kw_only=True, compare=False)
```

`dataclasses.field()`의 `kw_only` 옵션은 **Python 3.10부터** 지원된다.
이 저장소를 돌리는 환경은 Python 3.9.13이라 `nodes` 패키지를 import하는
순간 아래 오류로 죽는다.

```
TypeError: field() got an unexpected keyword argument 'kw_only'
```

`pytest tests/`가 수집(collect) 단계에서부터 실패해서 **0개 테스트도
실행되지 않는다.** 직접 재현 확인함.

- 도입 커밋: `cdb4db7` ([fix] 디버그 모드 메시지 포맷 및 줄 번호/breakpoint
  정확도 개선, 2026-07-08), `54c6575`에서 ImportStmt에도 동일 패턴 추가.
- 팀 전체가 Python 3.10+ 환경에서만 작업 중이라면 실제로는 문제가 안 될
  수 있으나, 3.9 환경이 하나라도 있다면 그 환경에서는 아무 작업도 검증할
  수 없다. **Python 버전을 3.10+로 통일하거나, `kw_only` 없이 필드 순서를
  조정하는 방식으로 수정이 필요하다.**

## 기능별 진행 상황

### function — 완료

| 영역 | 상태 |
| --- | --- |
| Assembler (파싱) | 완료 (`_statement_parser.py`, `_expression_parser.py`) |
| Checker (정적 오류 2종) | 완료 (`checker.py`) |
| Executor (실행, 재귀 포함) | 완료 (`_function.py`의 `Function`/`LoxCallable`, `_signals.py`의 `ReturnSignal`) |

### class — instanceof만 미완료

| 영역 | 상태 |
| --- | --- |
| 클래스 선언/인스턴스 생성 | 완료 |
| 필드 읽기/쓰기 | 완료 (`FieldGetExpr`/`FieldSetExpr`) |
| 메서드/생성자(init)/상속/super | 완료 (`LoxClass`/`LoxInstance`, `_class.py`) |
| Checker 정적 오류 5종 | 완료 |
| 런타임 오류 4종 | 완료 (`NotAClassError`, `NotAnInstanceError`, `UndefinedPropertyError`) |
| **instanceof 연산자** | **부분구현** — Node(`InstanceOfExpr`)와 Executor 평가(`_evaluate_instanceof`)는 있지만, **Assembler가 `instanceof`를 파싱해서 `InstanceOfExpr`를 만들지 않는다.** 소스 코드에서 이 연산자를 쓸 방법이 아직 없음 (`test_ast_builder.py`에 "이 담당 범위가 아니므로 아직 파싱되지 않음"이라는 주석으로 명시되어 있음) |

### import — Assembler/Checker는 진행, Executor는 미착수

| 영역 | 상태 |
| --- | --- |
| Assembler (문법 파싱, 경로 STRING 제한) | 완료 |
| Checker — 같은 scope 중복 import | 완료 |
| Checker — alias 이름 충돌 | 완료 |
| Checker — 반복문 내부 import | 완료 |
| Checker — **상위 level 중복 import 금지** | **미구현** — import.md 세부 규칙에 명시되어 있으나, 현재 `imported_paths`는 스코프별로만 관리되어 상위 스코프에서 이미 import한 파일을 하위에서 다시 import해도 잡히지 않는다 |
| Checker — import 문법 오류 / 파일 없음 / 순환 import | Assembler 담당으로 분류됨 (미검증, 별도 확인 필요) |
| Executor (파일 읽기/조립/alias로 실제 접근/순환 import 감지) | **미착수** — `src/executor/`에 `ImportStmt` 처리 코드가 전혀 없음 |

### 정적배열 — 완료

토큰, `IndexGetExpr`/`IndexSetExpr`, 런타임 오류 4종(`IndexOutOfRangeError`,
`InvalidIndexTypeError`, `NotAnArrayError`, `InvalidArraySizeError`),
`ArrayBuiltin`/`FabArray` 전부 구현되어 있다.

### 공장제어쉘 — 완료

3가지 실행 모드(Prompt/File/Debug, `_prompt_shell.py`의 `run_cli`/
`run_file`/`run_debug`)와 디버그 명령 10종(step/next/continue/breakpoint/
watch/inspect 등, `_debugger.py`) 전부 구현되어 있다.

### 실행전 최적화 — 미착수

`distance`/resolve/상수 폴딩 등 관련 로직이 `src/` 어디에도 없다. 착수 전.

## 앞으로 해야 할 일 (우선순위 순)

1. **[긴급] kw_only Python 버전 호환성 문제 해결** — 팀 Python 버전 통일 또는
   `nodes/stmt.py`의 `line` 필드 선언 방식 수정
2. instanceof — Assembler 파싱 추가 (Executor/Node는 이미 있음)
3. import Executor 구현 — 실제 파일 읽기·조립·alias 접근·순환 import 감지
4. import Checker에 "상위 level 중복 import 금지" 규칙 추가
5. 실행전 최적화 착수 (아직 아무도 시작 안 함)
